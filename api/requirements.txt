# Vercel Python serverless — api/design.py and api/health.py
# Only stdlib is used (json, os, base64, urllib) — no extra packages needed.
# List is kept explicit so Vercel's build does not fail on an empty file.
httpx==0.27.0
