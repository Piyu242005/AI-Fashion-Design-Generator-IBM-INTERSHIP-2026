# Vercel Python serverless — api/design.py, api/health.py, api/try-on.py
# api/design.py and api/health.py use only stdlib.
# api/try-on.py requires gradio_client to talk to the HF IDM-VTON Space.
httpx==0.27.0
gradio_client==1.3.0
