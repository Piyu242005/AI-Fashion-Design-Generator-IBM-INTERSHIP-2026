# ── Core AI / deep learning ───────────────────────────────────────────────
# Install torch with the correct CUDA wheel before running pip install:
#   pip install torch==2.1.0 torchvision --index-url https://download.pytorch.org/whl/cu118
torch==2.1.0
torchvision
accelerate==0.21.0

# ── Hugging Face ecosystem ────────────────────────────────────────────────
diffusers==0.25.0
transformers==4.30.2
huggingface_hub>=0.20.0
safetensors==0.3.1

# ── Segment Anything (install from source separately) ────────────────────
# pip install 'git+https://github.com/facebookresearch/segment-anything.git'
# segment-anything

# ── Detectron2 (install from source separately) ──────────────────────────
# pip install 'git+https://github.com/facebookresearch/detectron2.git'
# detectron2

# ── Image processing ──────────────────────────────────────────────────────
Pillow==9.5.0
opencv-python
scikit-image==0.21.0

# ── Numerical / ML utilities ─────────────────────────────────────────────
numpy==1.23.5
einops==0.4.1
kornia==0.6.7
timm==0.9.2
open-clip-torch==2.20.0
clip

# ── ONNX runtime (human parsing models) ──────────────────────────────────
onnxruntime==1.16.2

# ── Quantisation / memory optimisation ───────────────────────────────────
bitsandbytes==0.39.0

# ── Diffusion / sampling utilities ───────────────────────────────────────
pytorch_lightning==1.9.4
torchdiffeq==0.2.3
torchsde==0.2.6
omegaconf==2.2.3
tomesd==0.1.3

# ── Computer vision extras ────────────────────────────────────────────────
basicsr==1.4.2
realesrgan==0.3.0
gfpgan==1.3.8
resize-right==0.0.2
clean-fid==0.1.35

# ── Web / API utilities ───────────────────────────────────────────────────
fastapi==0.94.0
gradio==3.41.2
httpcore==0.15
httpx==0.24.1

# ── Streamlit UI ─────────────────────────────────────────────────────────
streamlit>=1.32.0

# ── Environment variable management ─────────────────────────────────────
python-dotenv>=1.0.0

# ── General utilities ─────────────────────────────────────────────────────
GitPython==3.1.32
blendmodes==2022
inflection==0.5.1
jsonmerge==1.8.0
lark==1.1.2
piexif==1.1.3
psutil==5.9.5
fvcore
cloudpickle
pycocotools
tqdm
av
