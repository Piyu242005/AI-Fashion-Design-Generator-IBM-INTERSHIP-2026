# ============================================================
#  requirements.txt
#  Piyu AI Clothing Fashion Design Generator
#
#  Python 3.10, 3.11, or 3.12 supported.
#  auto1111sdk has been removed — RealVisXL is loaded via Diffusers.
#
#  Install order:
#    1. pip install torch==2.1.0 torchvision==0.16.0 \
#           --index-url https://download.pytorch.org/whl/cu118
#    2. pip install -r requirements.txt
# ============================================================

# ── Core AI / deep learning ───────────────────────────────────────────────
torch==2.1.0
torchvision==0.16.0
accelerate>=0.30.0

# ── Hugging Face ecosystem ────────────────────────────────────────────────
diffusers>=0.27.2
transformers>=4.37.2
huggingface_hub>=0.23.0
safetensors>=0.4.3
tokenizers>=0.15.2
peft>=0.10.0

# ── OpenAI CLIP (required by SAM, IDM-VTON, open-clip) ───────────────────
# Install from source — the PyPI 'clip' package is a different project.
# Run:  pip install git+https://github.com/openai/CLIP.git
# It is listed here as a comment because pip install -r cannot handle git URLs
# on all platforms; run the git URL command manually or in the Dockerfile.

# ── Open-CLIP (used by some model extras) ────────────────────────────────
open-clip-torch>=2.20.0

# ── Segment Anything (install from source) ───────────────────────────────
# pip install git+https://github.com/facebookresearch/segment-anything.git

# ── Image processing ──────────────────────────────────────────────────────
Pillow>=9.5.0,<11.0
opencv-python>=4.9.0
scikit-image>=0.21.0

# ── Numerical / ML utilities ─────────────────────────────────────────────
numpy>=1.24.4,<2.0
einops>=0.7.0
kornia>=0.6.7
timm>=0.9.16

# ── ONNX runtime (human parsing models) ──────────────────────────────────
onnxruntime>=1.16.3

# ── Quantisation / memory optimisation ───────────────────────────────────
# bitsandbytes: Windows needs the pre-built wheel — install with --prefer-binary
bitsandbytes>=0.41.0

# ── Diffusion / sampling utilities ───────────────────────────────────────
pytorch_lightning>=1.9.4,<2.0
torchdiffeq>=0.2.3
torchsde>=0.2.6
omegaconf>=2.2.3
tomesd>=0.1.3

# ── Computer vision extras ────────────────────────────────────────────────
basicsr>=1.4.2
realesrgan>=0.3.0
gfpgan>=1.3.8
resize-right>=0.0.2
clean-fid>=0.1.35

# ── Web / API utilities ───────────────────────────────────────────────────
fastapi>=0.94.0
gradio>=3.41.2
httpcore>=0.15
httpx>=0.24.1

# ── Streamlit UI ─────────────────────────────────────────────────────────
streamlit>=1.32.0

# ── Environment variable management ─────────────────────────────────────
python-dotenv>=1.0.0

# ── General utilities ─────────────────────────────────────────────────────
GitPython>=3.1.32
inflection>=0.5.1
jsonmerge>=1.8.0
lark>=1.1.2
piexif>=1.1.3
psutil>=5.9.5
cloudpickle
pycocotools
fvcore
tqdm
av
