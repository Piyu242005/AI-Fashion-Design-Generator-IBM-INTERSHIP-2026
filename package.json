{
  "type": "module",
  "scripts": {
    "dev": "npx vercel dev",
    "build": "vite build",
    "preview": "vite preview",
    "test": "python -m pytest -q",
    "test:coverage": "python -m pytest --cov=api --cov-report=term-missing",
    "ci": "npm run build && npm test"
  },
  "dependencies": {
    "lucide-react": "^0.460.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@vitejs/plugin-react": "^6.0.5",
    "tailwindcss": "^4.3.3",
    "vite": "^8.2.1"
  }
}
